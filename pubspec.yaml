name: click_to_share
description: A new Flutter application.
version: 1.0.8+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^17.0.0
  permission_handler: ^11.3.1
  cupertino_icons: ^1.0.2
  # Versi di bawah ini sudah mendukung Null Safety
  camera: ^0.10.5
  path_provider: ^2.1.1
  path: ^1.8.3
  # Library share yang lebih modern karena esys_flutter_share sudah usang
  share_plus: ^7.1.0 

flutter:
  uses-material-design: true